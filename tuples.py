#las TUPLES no varian

x=(1,2,3)
print(x)

#se pueden crear TUPLES dentro de TUPLES como también se crean LISTAS dentro de LISTAS

print(dir(x))

#colocando un solo elemento dentro de una TUPLE el INTERPRETE no la reconoce como tal. Debe colocarse una , para que se interprete de esa manera
l=(1)
k=(1,)
print(l)
print(k)

print(x[2])
#al igual que las LISTAS se puede traer un valor determinado mediante su número de INDICE

#del K
#para borrar la TUPLE se usa la funcion DEL. Nos dira que el valor de la TUPLE no esta definido como mensaje resultante ya que se borró

locations={
     (35.626985, 75.26598) : "tokio", 
     (15.362569, 26.65986) : "buenos aires"
}
#las TUPLES pueden usarse dentro de DICCIONARIOS entre otras aplicaciones






























